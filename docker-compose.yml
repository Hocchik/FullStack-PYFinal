version: '3.8'

services:
  backend:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - sqlserver
    environment:
      - DB_USER=UsuarioA
      - DB_PASS=papaya
      - DB_NAME=pedidos
      - DB_HOST=sqlserver

  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      SA_PASSWORD: "Papaya123!"
      ACCEPT_EULA: "Y"
    ports:
      - "1433:1433"